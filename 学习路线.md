# pi 源码学习路线（面向 Agent 开发学习者）

> 本文档基于对仓库 `D:\vue_workspace\pi` 的完整源码探索整理而成。
> 目标读者：正在学习 AI Agent 开发、希望以一个生产级开源 agent 项目为参照建立完整知识体系的工程师。
> 项目版本：所有包 0.84.4（锁步版本），TypeScript（ESM），Node >= 22.19。

---

## 一、这个项目是什么，为什么值得学

pi 是一个**自扩展（self-extensible）的编码 agent 命令行工具**（类似 Claude Code / Codex CLI 的开源实现），由 Mario Zechner（badlogic）开发。它不是一个单一大文件项目，而是一个**分层清晰的 monorepo**：从"和 LLM 通信"到"agent 循环"到"终端 UI"到"远程会话"，每一层都是独立发布、可单独学习的 npm 包。

**对学习 agent 开发的价值（为什么值得精读）：**

1. **完整的分层解剖标本**。大多数 agent 教程只讲"prompt + 工具调用循环"这一小段，而 pi 把一个真实 agent 产品拆成了 10 个包：LLM 适配层（ai）→ 运行时（agent）→ 产品层（coding-agent）→ UI 层（tui）→ 持久化（session-backends）→ 远程协议（protocol/client/server）。你可以清楚地看到"每一层各自解决什么问题"。
2. **核心循环只有约 120 行**。`packages/agent/src/agent-loop.ts` 里的 `runLoop()` 是整个系统的心脏，短到可以一次读完，但围绕它的工程决策（事件流、中断、压缩、持久化）每一处都值得深挖。
3. **"最小核心 + 扩展一切"的架构哲学**。pi 刻意不内置 MCP、子 agent、权限弹窗、计划模式、待办清单——这些全部用扩展（extension）实现，仓库里附带约 80 个扩展示例。这是学习"如何设计可扩展系统"的绝佳材料。
4. **大量生产级工程细节**：流式输出永不抛错、上下文压缩的切点选择、会话树的崩溃恢复、跨 provider 会话迁移时的消息变换、SQLite 写者租约防脑裂……这些是教程里学不到的。

**最重要的设计哲学（先记住，后面处处体现）：**

> 核心保持极小。如果一个功能不属于核心，它就应该是一个扩展（`packages/coding-agent/docs/extensions.md`），而不是往核心包里加东西。

**配套代码导读**：[代码导读/](代码导读/README.md) 目录提供核心源码的**中文逐段注释版**（原样摘录代码 + 行内中文注释 + Java 视角对照），共五篇：[01-agent-loop.md](代码导读/01-agent-loop.md)（agent 循环的心脏）、[02-agent.md](代码导读/02-agent.md)（Agent 类与事件屏障）、[03-agent-types.md](代码导读/03-agent-types.md)（运行时全部契约）、[04-compaction.md](代码导读/04-compaction.md)（上下文压缩）、[05-sdk.md](代码导读/05-sdk.md)（产品层组合根）。读阶段 2、3 时配合使用——开两个窗口，左边注释版、右边源码原版对照。

---

## 二、全局架构：包依赖与分层

### 2.1 依赖关系图（自底向上）

```
（无内部依赖，完全独立）
  tui          终端 UI 库（差分渲染）
  telemetry    遥测契约（零运行时依赖）

  ai           统一多 provider LLM API
   ↑ 依赖 telemetry（仅类型）
  agent        Agent 运行时（工具循环、事件流、会话、压缩、内置工具）
   ↑ 依赖 ai、telemetry
  session-backends/sqlite-node   SQLite 会话后端
   ↑ 依赖 agent、ai

  protocol     远程会话协议（CBOR + typebox schema）
   ↑ 依赖 typebox
  client       远程会话客户端
   ↑ 依赖 protocol
  server       实验性会话服务器
   ↑ 依赖 protocol、ai

  coding-agent pi CLI 本体（最终产品）
   ↑ 依赖 agent、ai、tui、client、protocol（注意：不依赖 server）
  evals        评测框架（私有包）
```

**分层要点（每一层"为什么存在"）：**

| 层 | 包 | 解决的问题 |
| --- | --- | --- |
| 通信层 | `ai` | 各家 LLM 的 API 格式完全不同（Anthropic Messages、OpenAI Responses/Completions、Bedrock Converse、Google……）。统一成一套消息模型 + 一套流式事件协议 + 一套鉴权抽象，上层代码完全不感知 provider 差异。 |
| 运行时层 | `agent` | "调用 LLM → 执行工具 → 再调用 LLM"的循环、事件流、中断（steering/abort）、有状态包装、会话持久化模型、上下文压缩。与 UI 无关、与宿主无关（能跑在 Node/浏览器/沙箱）。 |
| 产品层 | `coding-agent` | 把运行时变成真正的 CLI 产品：内置工具、系统提示词组装、扩展系统、TUI 交互、配置管理、会话管理。 |
| 表现层 | `tui` | 终端里无闪烁地渲染流式输出（agent 每秒都在产出新 token，普通打印会闪成灾难）。 |
| 持久层 | `session-backends/*`、agent 内置的 JSONL 后端 | 会话怎么存才能支持分支、恢复、搜索。核心包定义接口（`SessionStorage`），具体后端可插拔。 |
| 远程层 | `protocol` / `client` / `server` | 让 agent 会话可以跑在远程机器上、被多个客户端共享。协议是传输中立的（只要保序字节流）。 |
| 观测层 | `telemetry` | 不绑定任何后端（OTel/Sentry/日志）的遥测契约，靠显式传参而非全局状态。 |

### 2.2 每个包的一句话定位

| 路径 | npm 包名 | 一句话 |
| --- | --- | --- |
| `packages/tui` | `@earendil-works/pi-tui` | 差分渲染终端 UI 库，提供组件/键盘/编辑器/Markdown 渲染 |
| `packages/telemetry` | `@earendil-works/pi-telemetry` | 厂商中立的遥测契约与类型化 schema 工具 |
| `packages/ai` | `@earendil-works/pi-ai` | 统一多 provider LLM API（41 家 provider、10 种线上协议） |
| `packages/agent` | `@earendil-works/pi-agent-core` | 有状态的 agent 运行时：工具执行 + 事件流 + 会话 + 压缩 |
| `packages/session-backends/sqlite-node` | `@earendil-works/pi-session-backend-sqlite-node` | 会话模型的 SQLite 生产级后端（含全文检索） |
| `packages/protocol` | `@earendil-works/pi-protocol` | 远程 pi 会话的 CBOR 二进制协议（schema + 编解码 + 分帧） |
| `packages/client` | `@earendil-works/pi-client` | 远程会话客户端（含会话租约语义） |
| `packages/server` | `@earendil-works/pi-server` | 实验性会话服务器框架（宿主实现服务接口） |
| `packages/coding-agent` | `@earendil-works/pi-coding-agent` | `pi` CLI 本身 |
| `packages/evals` | 私有 | 评测框架，`npm run eval` 运行 |

---

## 三、先建立全局观：一次提问的完整生命周期

在钻进任何包之前，先理解"用户按下回车之后发生了什么"。这条数据流贯穿所有核心包，是整个项目的骨架：

```
用户在终端输入 "帮我修复这个 bug"，按 Enter
   │
   ▼
【tui】Editor 组件捕获输入，onSubmit 回调触发，requestRender() 触发差分渲染
   │
   ▼
【coding-agent】interactive-mode.ts 判断：不是斜杠命令 → AgentSession.prompt(text)
   │
   ▼
【coding-agent】AgentSession 把用户消息写入会话、入队，驱动 Agent
   │
   ▼
【agent】Agent.prompt() → 启动 agentLoop（双层循环）
   │
   ├─ ① transformContext()：扩展可在此改写上下文（每次 LLM 调用前）
   ├─ ② convertToLlm()：内部 AgentMessage[] → pi-ai 的标准 Message[]
   ▼
【ai】Models.streamSimple() → Provider.stream() → api/<协议>.ts
      把统一 Context 翻译成某家 provider 的 HTTP 请求（SSE 流）
   │
   ▼  AssistantMessageEvent 流（text_delta / toolcall_delta / ...）
【agent】逐事件转发为 AgentEvent（message_update...），错误永不抛出、
      编码进 stopReason
   │
   ▼
【tui / coding-agent】interactive-mode 订阅事件流，Markdown 组件逐 token 渲染
   │
   ▼ 流结束，stopReason === "toolUse"
【agent】executeToolCalls()：
      先逐个预检（参数校验 typebox → beforeToolCall 钩子可拦截/block）
      → 通过者并发执行（每个工具自己 throw 表示失败）
      → afterToolCall 钩子可改写结果
      → 按原始顺序生成 toolResult 消息追加进上下文
   │
   ▼ 回到 ① 继续下一轮，直到 stopReason === "stop"（没有工具调用了）
【agent】agent_end 事件 → 事件屏障保证所有监听器（含会话落盘）完成后
      prompt() 的 Promise 才 resolve
   │
   ▼
【agent / coding-agent】检查上下文 token 是否逼近窗口上限
      → 是则触发 compaction（压缩）：LLM 生成结构化摘要替换旧历史
   │
   ▼
【coding-agent】SessionManager 把整棵会话树追加写入 JSONL 文件（可恢复/可分支）
```

记住四个关键词：**流式事件（streaming events）**、**工具循环（tool loop）**、**压缩（compaction）**、**会话树（session tree）**。整份学习路线就是把这四个词逐层展开。

---

## 四、学习路线总览

按依赖自底向上学（地基 → 上层），每阶段给出建议时长、学习目标、精读文件、练习。**核心是阶段 1~3；阶段 4~6 可按兴趣选学。**

| 阶段 | 包 | 主题 | 建议时长 | 优先级 |
| --- | --- | --- | --- | --- |
| 0 | 全局 | 跑通项目、读文档建立地图 | 0.5 天 | 必做 |
| 1 | `ai` | LLM 抽象层：消息模型、流式事件、provider 适配 | 3~5 天 | 必做 |
| 2 | `agent` | Agent 运行时：循环、事件流、工具、中断、压缩、会话 | 5~7 天 | 必做（最核心） |
| 3 | `coding-agent` | 产品层：启动链路、内置工具、系统提示词、扩展系统 | 7~10 天 | 必做（最核心） |
| 4 | `tui` | 终端 UI：差分渲染、组件、编辑器 | 3~4 天 | 选学 |
| 5 | `sqlite-node` | 会话存储的生产级后端：租约、分支缓存、FTS | 2 天 | 选学 |
| 6 | `protocol`/`client`/`server` | 远程会话协议三件套 | 3 天 | 选学 |
| 7 | `telemetry`/`evals`/harness.md | 观测、评测、下一代架构设计 | 持续 | 进阶 |

---

## 五、阶段 0：环境准备与跑通项目（0.5 天）

### 5.1 学习目标

- 项目能在本地跑起来；脑子里有一张"哪个目录管什么"的地图。

### 5.2 要做的事

1. **通读三份根级文档**：
   - `README.md`（根目录）——项目定位与快速上手。
   - `AGENTS.md`——本仓库的开发规则（构建顺序、代码规范、测试方式），同时也告诉你各包的角色表。
   - `CONTRIBUTING.md`——贡献门槛与质量标准，能看出维护者的工程品味。
2. **理解构建顺序即依赖顺序**：根 `package.json` 的 `build` 脚本按 `tui → telemetry → ai → agent → sqlite-node → protocol → client → server → coding-agent → evals` 编译，每个包只依赖排在前面的包。**这天然就是学习顺序。**
3. **跑通交互模式**（Windows 下用 `pi-test.bat` 或 `pi-test.ps1`，POSIX 下 `./pi-test.sh`）：随便问一个问题、观察流式输出和工具调用渲染，建立感性认识。
4. **浏览 `packages/coding-agent/docs/`**（30 篇文档，先只看标题）：`extensions.md`（扩展权威文档）、`sdk.md`、`compaction.md`、`session-format.md`、`security.md`、`providers.md`、`rpc.md`……后续阶段会反复回来精读。

### 5.3 常用命令速查

| 命令 | 作用 |
| --- | --- |
| `npm run check` | 全量检查：lint + 类型检查 + 依赖规则（改代码后必跑） |
| `./test.sh` | 在隔离 HOME、无 API key 下跑非 e2e 测试 |
| `./pi-test.sh` / `pi-test.bat` | 从源码运行 pi |
| `npm run generate:models` | 重新生成 ai 包的模型目录 |

注意：不要直接跑 `npm test`（会激活 e2e），跑单个测试用：
`node "$(git rev-parse --show-toplevel)/node_modules/vitest/dist/cli.js" --run test/xxx.test.ts`（tui 包用 `node --test`）。

---

## 六、阶段 1：packages/ai —— 统一 LLM API 层（3~5 天）

### 6.1 这个包解决什么问题

各家 LLM 的 API 差异极大：Anthropic 用 Messages + SSE、OpenAI 有 Completions 和 Responses 两套、Bedrock 走 Converse、Google 有自己的 SDK；thinking/reasoning 参数每家叫法不同（`reasoning_effort` / `enable_thinking` / `thinking_budget`……）；鉴权有 API key、OAuth、AWS 环境凭证。如果上层 agent 代码直接对接这些，每换一家模型就要改一遍核心逻辑。

**pi-ai 的答案：三层解耦。**

```
Models（集合与鉴权编排：stream/complete/refresh/checkAuth...）
  └─ Provider（商业实体：目录 + 鉴权 + 行为，41 家）
       └─ API 实现（线上协议，src/api/ 下 10 种，多家 provider 共享同一协议实现）
```

例如 xAI、Groq、OpenRouter、DeepSeek 等数十家都共享 `openai-completions` 协议实现——因为它们都是 OpenAI 兼容端点。

### 6.2 必须吃透的核心概念

**(1) 统一消息模型**（`src/types.ts`）

- `Context = { systemPrompt?, messages: Message[], tools?: Tool[] }` —— 发给 LLM 的全部载荷，可直接 `JSON.stringify`。
- 三种角色：
  - `UserMessage`：用户消息，content 是字符串或内容块数组（文本/图片）。
  - `AssistantMessage`：模型回复，content 是 `TextContent | ThinkingContent | ToolCall` 的数组，带 `usage`、`stopReason`、`model` 等元数据。
  - `ToolResultMessage`：工具结果，content 可含文本**和图片**，带 `isError` 标志。
- `StopReason = "pending" | "stop" | "length" | "toolUse" | "error" | "aborted" | "deferred"` —— 整个系统靠它驱动循环：`toolUse` → 继续执行工具；`stop` → 结束；`length` → 输出被截断。

**(2) 统一流式事件**（`src/types.ts` 的 `AssistantMessageEvent` + `src/utils/event-stream.ts`）

无论底层是哪家 SSE，消费者看到的都是同构事件序列：

```
start → (text_start → text_delta → text_end)*
      → (thinking_start → thinking_delta → thinking_end)*
      → (toolcall_start → toolcall_delta → toolcall_end)*
      → done | error
```

每个事件携带 `partial`（持续更新的部分 AssistantMessage）和 `contentIndex`（事件流不保证内容块连续，必须用下标关联）。

**两条黄金约定（整个 monorepo 的基石）：**
- **流函数绝不 throw**：所有错误被编码为 `stopReason: "error" | "aborted"` 的 AssistantMessage，从流的 `result()` 返回。这样 UI/循环层永远不需要 try-catch 流本身。
- **返回流而不是 Promise**：`stream()` 同步返回 `AssistantMessageEventStream`，异步的鉴权在流"后面"跑，失败以 error 事件终止。

**(3) `Models` / `Provider` / `createProvider`**（`src/models.ts`）

- `Models` 是 provider 集合的门面：`stream/complete/streamSimple/completeSimple/refresh/checkAuth/login/logout`。
- `createProvider({...})` 用零件组装 provider，这是**自定义 provider 接入的标准姿势**（配合 coding-agent 的 `~/.pi/agent/models.json` 或扩展的 `registerProvider`）。
- `hasApi()` 是运行时类型守卫，把动态查到的模型收窄为 `Model<"anthropic-messages">` 等，让 stream options 全类型化。

**(4) 模型目录即代码**（`src/models.generated.ts`）

由 `scripts/generate-models.ts` 从 models.dev 数据自动生成（**永远不要手改**），把 40 家 provider 的模型目录聚合为带字面量类型的 `MODELS` 常量——好处是 `getBuiltinModel('openai', 'gpt-4o-mini')` 的返回类型精确到具体 API，模型 ID 还能自动补全。`compat` 字段矩阵描述每家端点的兼容开关（是否支持 developer role、thinking 格式、maxTokens 字段名等）。

**(5) token 与成本记账**

- `Usage`：`input/output/cacheRead/cacheWrite/reasoning` + `cost`，`calculateCost(model, usage)` 支持分层定价。
- `estimateContextTokens(context)`（`src/utils/estimate.ts`）：优先用最近一条 assistant 消息的**真实 usage**，只对其后的尾部消息按"4 字符 ≈ 1 token、图片 ≈ 4800 字符"估算——这个"锚点 + 尾部估算"的策略后面 compaction 直接复用。

### 6.3 精读顺序（文件清单）

| 顺序 | 文件 | 职责 | 看什么 |
| --- | --- | --- | --- |
| 1 | `src/types.ts`（859 行） | 所有核心类型 | 消息模型、事件、Tool、各 API 的 Options/Compat |
| 2 | `src/utils/event-stream.ts` | `EventStream<T,R>` 基类 | push/end/result 三件套，错误如何进流 |
| 3 | `src/models.ts` | Models/Provider/工厂 | `createProvider`、`hasApi`、`calculateCost` |
| 4 | `src/api/anthropic-messages.ts`（1391 行）或 `src/api/openai-completions.ts`（1707 行） | 挑一家协议实现精读 | 统一事件怎么从 SSE 翻译出来；错误怎么编码 |
| 5 | `src/api/simple-options.ts` | streamSimple 的共享基座 | maxTokens 收敛、thinking 预算（保证回答至少留 1024 token） |
| 6 | `src/api/transform-messages.ts` | **跨 provider 消息变换** | 换模型时 thinking 块降级、toolCallId 规范化、孤儿 tool call 补错误结果——真实工程细节极其密集，强烈推荐 |
| 7 | `src/utils/validation.ts` | 工具参数校验 | TypeBox 校验 + 类型强制转换，失败作为 isError 结果回喂 |
| 8 | `src/providers/faux.ts` | 确定性测试 provider | 脚本化响应队列，学 agent 时的"假模型" |
| 9 | `src/auth/` | 鉴权模型 | ApiKeyAuth.resolve / OAuth refresh 与 toAuth 拆分（串行化防并发双刷新） |

其余：`src/api/` 下还有 openai-responses、azure、codex、google-generative-ai/vertex、bedrock、mistral、pi-messages（pi 自有协议）；`src/utils/overflow.ts` 收录了 20+ 家 provider 的上下文溢出错误文案模式（有趣的"脏活"样本）；`src/utils/json-parse.ts` 提供流式渐进 JSON 解析（工具参数边生成边 parse）。

### 6.4 动手练习

1. 参考 `packages/ai/test/` 下的测试，用 `fauxProvider` 写一个最小脚本：构造 Context → streamSimple → 打印每种事件。
2. 用 `createProvider` 接入一个 OpenAI 兼容的本地模型（如 Ollama），体会"自定义 provider"的接入成本有多低。
3. （有 key 的话）真实调用一次，打印 `usage`，观察 cacheRead/cacheWrite。

---

## 七、阶段 2：packages/agent —— Agent 运行时（5~7 天，全仓库最核心）

### 7.1 这个包解决什么问题

有了 LLM 调用能力后，"agent"还缺：循环（调模型 → 跑工具 → 再调）、事件流（UI 要能渲染过程）、中断（用户要能插话/停止）、状态与持久化（会话要能恢复）、上下文管理（窗口会满）。`pi-agent-core` 把这些做成**与 UI 无关、与宿主无关**的运行时库，分三层：

```
第一层：agentLoop()          低层纯函数式循环，返回事件流（只观察，不管持久化）
第二层：Agent 类              有状态包装：队列、abort、事件屏障（生产用）
第三层：src/harness/          编码 agent 外围件：会话树、压缩、内置工具、skills
```

**消息流设计（贯穿全包的主线）：**

```
AgentMessage[]（可扩展的内部消息）
   → transformContext()（剪枝/压缩/扩展改写）
   → convertToLlm()（边界转换）
   → Message[]（pi-ai 标准消息）
   → LLM
```

`AgentMessage` 通过 TS 的 declaration merging 可注入自定义消息类型（harness 自己就注册了 `bashExecution`、`custom`、`branchSummary`、`compactionSummary` 四种）——这是"内部表示丰富、对外协议收敛"的经典手法。

### 7.2 必须吃透的核心概念

**(1) AgentEvent：嵌套事件协议**（`src/types.ts`）

```
agent_start
  ├─ turn_start
  │    ├─ message_start / message_update(仅assistant) / message_end
  │    ├─ tool_execution_start / tool_execution_update / tool_execution_end
  └─ turn_end
agent_end
```

这是理解一切 agent UI 的基础：UI 层订阅这棵事件树就能渲染完整过程，而不需要轮询状态。

**(2) agent loop 的双层循环**（`src/agent-loop.ts` 的 `runLoop()`，约 120 行）

- **内层循环**：还有未执行的 toolCall / 待注入消息时继续——
  1. `prepareNextTurn()`：每轮开始前可整体替换 context/model/thinking level（compaction 就靠它）；
  2. 注入 steering 消息（用户运行中插的话）；
  3. `streamAssistantResponse()`：`transformContext → convertToLlm → streamFn`，逐事件转发；
  4. 若 `stopReason === "length"`（输出截断，工具参数可能不完整）→ **整批工具调用标记失败**让模型重发（细节！）；
  5. 否则 `executeToolCalls()`；
  6. `turn_end` → `shouldStopAfterTurn()` 返回 true 则优雅退出（compaction 触发点）。
- **外层循环**：本来要停时检查 `getFollowUpMessages()`，有排队消息就再跑一轮。

**(3) 工具三段式执行**（`executeToolCalls`）

```
预检（顺序）：prepareArguments（兼容 shim）→ validateToolArguments（TypeBox）→ beforeToolCall（可 block）
执行（并发）：通过预检的调用并发跑 execute()，完成即发 tool_execution_end（完成序）
收尾（顺序）：afterToolCall 可字段级改写结果；toolResult 消息按 assistant 原始顺序发出（持久化序）
```

三个关键约定：
- 工具**失败要 throw**，不要把错误写进 content——循环会捕获并转成 `isError: true` 的 toolResult 喂回模型；
- 工具可通过 `onUpdate` 回调流式上报进度（bash 工具 100ms 节流上报输出）；
- `executionMode: "sequential" | "parallel"` 按工具声明，批内任一 sequential 则整批顺序执行。

**(4) 三种中断模型**（人在环中的关键差异）

| 机制 | 语义 | 实现位置 |
| --- | --- | --- |
| `steer(msg)` | 运行中插话：当前批工具全部执行完、下次调 LLM 前注入 | `PendingMessageQueue` + 循环内轮询点 |
| `abort()` | 硬中断：AbortController，signal 贯穿流和每个工具的 execute | `Agent.abort()` |
| `followUp(msg)` | 追发：agent 本来要停时注入并再跑一轮 | 外层循环检查 |

**(5) Agent 类的事件屏障**（`src/agent.ts` 的 `processEvents()`）

每个事件先 reduce 内部状态，再**按注册顺序 await 每个监听器**。因此订阅 `agent_end` 的监听器（如"把会话刷到磁盘"）完成后，`prompt()` 返回的 Promise 才 resolve。**这就是裸 `agentLoop`（观察用）与 `Agent` 类（生产用）的本质区别**——持久化的正确性依赖这个屏障。

**(6) 上下文压缩 compaction**（`src/harness/compaction/compaction.ts`，848 行，教科书级实现）

- **何时压**：`estimateContextTokens()`（真实 usage 锚点 + 尾部估算）> contextWindow − reserveTokens（默认保留 16384）。默认 `keepRecentTokens: 20000`。
- **在哪切**：从尾部往回累计 token 到 keepRecentTokens，对齐到**合法切点**（只能切在 user/assistant/custom 消息边界，不能切在 toolResult 前）；切在回合中间时（isSplitTurn）把回合前缀单独摘要。
- **怎么压**：把待压缩历史喂给 LLM，用固定格式的结构化提示词生成摘要——`## Goal / Constraints & Preferences / Progress(Done|In Progress|Blocked) / Key Decisions / Next Steps / Critical Context` 六段；已有旧摘要时走**增量更新**（PRESERVE/ADD/UPDATE 规则）；同时从历史工具调用中提取"读过哪些文件、改过哪些文件"清单附进摘要（跨代继承）。
- **怎么恢复**：压缩结果作为 `compaction` 类型的 Entry 存进会话树；下次重建上下文时，`[compactionEntry, ...之后的entries]` 整体替换之前的历史——**原始历史不删除**，随时可回看/回退。

**(7) 会话树模型**（`src/harness/session/`，pi 最有特色的设计）

会话不是线性数组，而是**只追加的树 + 操作日志**：

- `Entry`（树节点，7 种类型：`message` / `model_change` / `thinking_level_change` / `active_tools_change` / `compaction` / `branch_summary` / `custom`），共同字段 `{type, id, seq, parentId, timestamp}`——**靠 parentId 就能原地分叉**，天然支持"从历史某点重试"。
- `LaneRecord`（与 Entry 分离的操作寄存器）：`operation_started/finished`、`abort_requested`、`tool_started`、`queue_enqueued`、`usage` 等——用于崩溃恢复（`reducer.ts` 把日志重放回状态）。
- 接口三件套：`Session`（领域门面）/ `SessionStorage`（后端契约）/ `SessionRepo`（仓库契约）。内置三个后端：内存（测试）、JSONL（默认文件）、SQLite（生产，见阶段 5），`testing/conformance.ts` 保证三者行为一致。
- `context.ts` 的 `buildSessionContext()`：从分支路径（沿 parentId 向根扫描）重建可发给 LLM 的上下文，途中把 compaction/branch_summary 投影为对应消息。

**(8) 内置工具与 ExecutionEnv**（`src/harness/tools/`）

bash/read/edit/write 四个工具的工厂函数。所有工具不直接碰 `node:fs`/`child_process`，而是通过 `ExecutionEnv = FileSystem & Shell` 接口（所有方法返回 `Result<T, Error>`、绝不 throw）。Node 实现在 `harness/env/nodejs.ts`。**这使同一套工具可跑在沙箱/远程环境**——coding-agent 的 ssh、gondolin 沙箱扩展就建立在这上面。

### 7.3 精读顺序（文件清单）

| 顺序 | 文件 | 职责 |
| --- | --- | --- |
| 1 | `README.md` | "Event Flow" 两张时序图——先看图再读码 |
| 2 | `src/types.ts` | StreamFn / AgentMessage / AgentTool / AgentToolResult / AgentEvent / AgentLoopConfig / AgentState 全部核心类型 |
| 3 | `src/agent-loop.ts` | **全仓库最重要的一个文件**：双层循环、流式转发、工具三段式、截断处理 |
| 4 | `src/agent.ts` | Agent 类：状态、队列、abort、事件屏障 `processEvents()` |
| 5 | `src/harness/tools/bash.ts`（+ read/edit/write） | 完整工具示范：流式 onUpdate、输出截断（2000 行/50KB，超限落盘临时文件）、错误约定、同文件写串行化（`file-mutation-queue`） |
| 6 | `src/harness/compaction/compaction.ts` | 压缩全流程（触发/切点/摘要/增量） |
| 7 | `src/harness/session/types.ts` → `session.ts` → `context.ts` | 树模型 → 门面 → 上下文重建 |
| 8 | `src/harness/skills.ts` / `prompt-templates.ts` / `system-prompt.ts` | SKILL.md 扫描加载、`/模板`、skills 注入系统提示的 XML 块 |
| 9 | `src/harness/reducer.ts` + `docs/harness.md` | 崩溃恢复重放 + 下一代架构规格书（2941 行，进阶） |

辅助：`src/proxy.ts`（浏览器经后端代理的 StreamFn——运行时解耦的示范）；`src/search/`（会话搜索接口与扫描实现）。

### 7.4 动手练习

1. 只用 `agentLoop` + `fauxProvider`：写 50 行脚本，跑通"两次工具调用 + stop"，把事件树完整打印出来。
2. 给 loop 注入一个自定义工具（比如"查天气"的假实现），观察 `beforeToolCall` 拦截和 `afterToolCall` 改写的效果。
3. 构造一段超过 keepRecentTokens 的假历史，手动调用 `prepareCompaction` + `compact`，检查摘要和 retainedTail。
4. 阅读并运行 `packages/agent/test/` 下的 session 相关测试，观察 JSONL 后端落盘的文件内容。

---

## 八、阶段 3：packages/coding-agent —— pi CLI 本体（7~10 天，与阶段 2 并列为核心）

### 8.1 这个包解决什么问题

运行时库不等于产品。用户要的是：装上就能用的 CLI、好用的工具集、合理的系统提示词、可配置、可扩展、可恢复会话、终端里的好体验。coding-agent 把所有"产品决策"集中在这里——**同时坚持最小核心哲学**：无 MCP、无子 agent、无权限弹窗、无 plan mode、无内置 todo，全部留给扩展（仓库附 80 个示例证明可行）。

### 8.2 启动链路（先走通一遍）

```
src/cli.ts（bin 入口：process.title、undici dispatcher）
  → src/main.ts 的 main()
     ├─ pi auth / pi config / pi install... 子命令
     ├─ parseArgs()（src/cli/args.ts，447 行）
     ├─ resolveAppMode()：interactive（TTY 默认）/ print（-p）/ json（--mode json）/ rpc（--mode rpc）
     ├─ createSessionManager()：--session/--continue/--resume/--fork 处理
     ├─ 项目信任解析（ProjectTrustStore → ~/.pi/agent/trust.json）
     ├─ createAgentSessionRuntime()：工厂模式组装运行时（会话可整体热切换，/tree、/fork 不重启进程）
     └─ 分发到 src/modes/ 对应模式
```

**与 agent 包的衔接点**（全包最重要的文件）：`src/core/sdk.ts` 的 `createAgentSession()` 里

```ts
const agent = new Agent({
  initialState,          // 系统提示词 + 消息 + 工具（来自会话/设置解析）
  convertToLlm,          // core/messages.ts：内部消息 → pi-ai 消息（可过滤图片）
  streamFn,              // 委托 ModelRuntime.streamSimple + 扩展的
                         // before_provider_headers / after_provider_response 事件
  transformContext,      // 每次 LLM 调用前让扩展改写消息
  ...
});
```

外面再套一层 **`AgentSession`**（`src/core/agent-session.ts`，3516 行）：补上会话持久化、系统提示词重建、工具注册表、自动压缩、模型切换、扩展绑定。这层是"生产级 agent 编排"的范本。

### 8.3 目录地图（src/ 实际布局）

```
src/
├── cli.ts / main.ts / index.ts / config.ts    入口三件套 + 路径解析（~/.pi/agent/...）
├── cli/          参数解析、auth 命令、@file 文件参数、会话选择器、项目信任 UI
├── core/         ★ 全部核心逻辑（见下表）
├── modes/        4 种运行模式：interactive/（TUI，主体 6575 行 + 45 个组件）、
│                 print-mode.ts、json-event.ts、rpc/
├── extensions/   随包内置隐藏扩展：llama.cpp 本地模型管理
├── client/       远程会话客户端（RemoteSession 状态机 + 转录折叠）
├── server/       create-harness.ts（服务端组装）
├── bun/          bun 编译二进制入口
└── utils/        ~30 个工具模块（跨平台 shell、git、图片缩放、剪贴板、frontmatter、语法高亮...）
```

**core/ 逐模块职责表（心脏）：**

| 模块 | 职责（解决什么问题） |
| --- | --- |
| `agent-session.ts` | 会话编排：prompt/steer/followUp/abort/compact/setModel；双消息队列；**自动压缩三触发**（manual=用户 /compact、threshold=接近上限、overflow=溢出恢复重试）；工具启停后系统提示词联动重建 |
| `agent-session-runtime.ts` / `agent-session-services.ts` | 运行时整体替换（切会话/分叉/树导航不重启）与一次性服务组装 |
| `tools/` | 8 个内置工具 + `file-mutation-queue`（同文件并发写串行化）+ 输出截断 |
| `extensions/` | 扩展系统四件套：`types.ts`（ExtensionAPI，1791 行）、`loader.ts`（jiti 加载 TS）、`runner.ts`（事件分发）、`wrapper.ts`（扩展工具包装为 AgentTool） |
| `compaction/` | CLI 层压缩实现（1012 行）：与 agent 包 harness 版同源思路 + overflow 恢复 |
| `settings-manager.ts` | 全局 `~/.pi/agent/settings.json` + 项目 `.pi/settings.json`（需信任）合并，约 60 个配置项 |
| `session-manager.ts` | JSONL 树状会话读写：buildSessionContext/getBranch/forkFrom/listAll、v1→v3 迁移、文件锁 |
| `resource-loader.ts` | 统一发现/加载：扩展、skills、提示模板、主题、**上下文文件（AGENTS.md/CLAUDE.md）** |
| `system-prompt.ts` | `buildSystemPrompt()` 系统提示词组装（见 8.5） |
| `slash-commands.ts` | 约 30 个内置斜杠命令表（/model /tree /compact /fork /share ...） |
| `model-runtime.ts` + model-*.ts | ModelRuntime：内置 provider + `models.json` 自定义 + 扩展注册 + 远端目录刷新；CLI 模型解析（`--model provider/id:thinking`） |
| `auth-storage.ts` / `runtime-credentials.ts` | auth.json（OAuth 令牌 + API key）存储与刷新 |
| `project-trust.ts` / `trust-manager.ts` | 项目信任状态机（安全模型核心，见 8.6） |
| `messages.ts` | 内部消息类型（CustomMessage/BashExecutionMessage）+ convertToLlm |
| `package-manager.ts` / `pi-manifest.ts` | pi 包（npm/git）安装到 `~/.pi/agent/`，`pi` 清单声明 extensions/skills/prompts/themes 路径 |
| `export-html/` | 会话导出 HTML（模板 + ANSI→HTML + 工具渲染器） |

### 8.4 内置工具（8 个）——工具实现的教科书

工具名：`read | bash | powershell | edit | write | grep | find | ls`。默认启用前四个（`createCodingTools`）；只读组合 `read+grep+find+ls` 用于审查场景。

每个工具都是同一个模式：**TypeBox schema + ToolDefinition + 可插拔 Operations 接口**：

| 工具 | 要点 |
| --- | --- |
| `read` | 图片 MIME 探测（自动缩放到 2000x2000、非视觉模型给占位提示）；行数/字节双限制截断；`ReadOperations` 接口可被扩展替换 |
| `bash` | `spawn` + 平台 shell 配置；**detached 进程组 + 杀进程树**（防孤儿进程）；流式输出；超时上限；向子进程注入 `PI_SESSION_ID/PI_MODEL` 等环境变量；`user_bash` 扩展事件可整体替换执行 |
| `powershell` | Windows 专属等价物 |
| `edit` | `{path, edits:[{oldText,newText}]}` 多次替换都**针对原始文件**匹配（要求唯一、不重叠）；统一 diff 生成；过 mutation queue |
| `write` | 简单写文件 + 递归建目录，同样过 mutation queue |
| `grep`/`find`/`ls` | 纯 TS 实现，尊重 .gitignore |

`tool-definition-wrapper.ts` 把 ToolDefinition 适配为 agent 包的 AgentTool 并注入扩展上下文——**一个定义，两个世界**（模型看到 schema，UI 看到 renderCall/renderResult，系统提示词看到 promptSnippet）。

### 8.5 上下文工程：系统提示词怎么组装（`core/system-prompt.ts`）

`buildSystemPrompt()` 的产出结构（顺序即优先级）：

1. 定位句（"You are an expert coding assistant operating inside pi..."）；
2. **Available tools**：只列提供了 `promptSnippet` 的工具（自定义工具不提供就不出现）；
3. Guidelines：按启用工具动态生成（如只有 bash 时提示"用 bash 做 ls/rg/find"）；
4. **pi 自文档指引**：注入 README/docs/examples 的绝对路径，用户问 pi 自身时模型自己去读对应文档——"模型知道自己的说明书"；
5. `appendSystemPrompt`（`--append-system-prompt` / APPEND_SYSTEM.md）；
6. **`<project_context>`**：AGENTS.md/CLAUDE.md 内容（`resource-loader.ts`：候选优先级 `AGENTS.override.md > AGENTS.md > CLAUDE.md`，从 cwd 逐级向上收集，全局→祖先→当前，含 git worktree 阴影去重）；
7. **`<available_skills>`**：只列 skill 的 name/description/location，指示模型**用 read 工具按需加载 SKILL.md 全文**——渐进式上下文注入，而非全量塞入；
8. 当前工作目录。

可替换途径：`.pi/SYSTEM.md`、`--system-prompt`、扩展的 `before_agent_start` 事件按回合替换。

### 8.6 安全模型（`docs/security.md`，立场清醒，值得一读）

**核心立场：没有内置沙箱、没有权限弹窗——真正的隔离必须来自操作系统/容器边界。** 具体三道防线：

1. **项目信任（唯一的输入加载防线）**：`.pi/` 下的 settings/extensions/skills/prompts、`.pi/SYSTEM.md` 等属于"项目可控输入"，首次使用需用户确认信任（`~/.pi/agent/trust.json` 持久化决策）。决策顺序：已保存决策 → `project_trust` 扩展事件 → `defaultProjectTrust` 设置。**为什么重要**：clone 一个恶意仓库时，其中的 `.pi/extensions/` 若无信任机制就会自动执行任意代码。
2. **工具审批 = `tool_call` 事件**：扩展监听该事件，返回 `{block: true, reason}` 即拦截——权限系统被降维成一个可扩展事件（官方示例：permission-gate.ts、protected-paths.ts、dirty-repo-guard.ts）。
3. **沙箱外置**：示例扩展 sandbox/（bash 沙箱）、ssh.ts（远程执行）、gondolin/（micro-VM）；docs/containerization.md 给容器模式。

### 8.7 扩展系统（本包最有特色的部分，`core/extensions/types.ts` + `loader.ts` + `runner.ts`）

扩展就是一个 TS 模块，默认导出 `(pi: ExtensionAPI) => void | Promise<void>`。用 **jiti** 直接加载 TypeScript（无需预编译）；编译二进制场景通过 VIRTUAL_MODULES 把 pi 的包注入为虚拟模块供扩展 import。

**ExtensionAPI 能力总览（约 45 种事件 + 注册函数）：**

- **事件订阅 `pi.on()`**：会话生命周期（session_start/before_switch/before_compact...）、agent 过程（context / before_provider_request / message_start/update/end / turn_* / tool_execution_*）、`tool_call`（**权限门的实现点**）、`tool_result`、`input`（改写用户输入）、`user_bash`……
- **注册**：`registerTool`（可替换内置工具）、`registerCommand`、`registerShortcut`、`registerFlag`（自定义 CLI 标志）、`registerProvider`（注册全新 LLM provider）、`registerMessageRenderer` / `registerMarkdownTransformer`。
- **动作**：`sendMessage` / `sendUserMessage` / `appendEntry`（持久化扩展状态）、`setModel`、`exec()`、`getActiveTools/setActiveTools`（工具动态启停）。
- **命令上下文的会话操作**：`newSession/fork/navigateTree/switchSession/reload`——**子 agent、plan mode 都是普通扩展基于这些原语实现的**（见 `examples/extensions/subagent/`、`plan-mode/`）。
- **UI 上下文 `ctx.ui`**：select/confirm/input、setStatus、setWidget（编辑器上下挂组件）、`setEditorComponent`（整体替换输入编辑器，如 vim 模式）、`custom()`（全焦点自定义组件）。

发现顺序：`~/.pi/agent/extensions/`（全局）→ `.pi/extensions/`（项目，需信任）→ pi 包（npm/git 安装）→ `-e` 显式路径。

### 8.8 精读顺序与练习

**精读顺序：**

1. `src/core/sdk.ts`（410 行）——最佳起点：Agent 如何被组合。
2. `src/core/agent-session.ts` ——生产级编排（可先读骨架：构造、prompt、_rebuildSystemPrompt、压缩触发三处）。
3. `src/core/tools/*.ts` ——read → bash → edit 顺序。
4. `src/core/system-prompt.ts` + `resource-loader.ts` + `skills.ts` ——上下文工程。
5. `src/core/session-manager.ts` + `docs/session-format.md` ——会话文件格式。
6. `src/core/extensions/types.ts` + `docs/extensions.md` ——扩展 API 全参考。
7. `src/modes/interactive/interactive-mode.ts` ——TUI 应用主体（可跳读：命令处理 + 事件渲染）。
8. `src/core/compaction/compaction.ts` + `docs/compaction.md`。

**练习（由浅入深）：**

1. 跑 `examples/sdk/01-minimal` 到 `13-session-runtime`（13 个渐进式 SDK 示例，本身就是教程）。
2. 写一个最小扩展：`registerCommand("hello", ...)`，放到 `.pi/extensions/` 跑通加载。
3. 写一个 `tool_call` 权限门扩展：拦截所有 `bash` 中的 `rm -rf`。
4. 写一个自定义工具（如"查询 git status"）并观察系统提示词里 promptSnippet 的注入效果。
5. 阅读 `examples/extensions/subagent/` 与 `plan-mode/`，理解"核心不做、扩展来做"如何落地。

### 8.9 官方文档地图（`packages/coding-agent/docs/`，30 篇）

主题 | 文档
--- | ---
扩展/SDK | `extensions.md`（必读）、`sdk.md`、`tui.md`
内部机制 | `compaction.md`、`session-format.md`、`sessions.md`
配置 | `settings.md`、`keybindings.md`、`providers.md`、`models.md`、`custom-provider.md`
模式 | `rpc.md`、`json.md`
安全 | `security.md`、`containerization.md`
平台 | `windows.md`、`termux.md`、`tmux.md`、`development.md`、`environment-variables.md`

---

## 九、阶段 4：packages/tui —— 终端 UI 库（3~4 天，选学）

### 9.1 解决什么问题

agent 每秒产出几十次 token 增量、工具输出持续滚动、用户还要能随时打字插话。普通"print + 清屏重画"会闪烁撕裂，React/Ink 又太重。pi-tui 的答案：**保留式渲染 + 行级差分**，外部依赖只有 `marked`（Markdown 解析）和 `get-east-asian-width`（中文宽度）。

### 9.2 核心机制

**(1) 组件契约（极简，`src/tui.ts`）**

```ts
interface Component {
  render(width: number): string[];   // 返回整屏行数组（每行含 ANSI，不得超过宽度）
  handleInput?(data: string): void;  // 获得焦点时收到原始终端输入（含转义序列）
  invalidate?(): void;               // 清缓存重绘
}
```

整个 UI 是组件树；每次渲染 `render(width)` 产出 `string[]`，与上一帧**逐行比较**得 firstChanged/lastChanged 区间，把硬件光标移到首个变化行、只重写这段——单行变化（spinner）只写一行。

**(2) 渲染管线的关键决策：**

- 16ms（≈60fps）节流合并普通重绘；**键盘输入立即渲染**（`requestImmediateRender`）；
- 全部写入包在 `\x1b[?2026h ... \x1b[?2026l`（同步输出）中，终端原子应用整批，杜绝中间态；
- 每行末尾强制 `SEGMENT_RESET`，样式不跨行泄漏；
- 两个渲染器：`TuiMainScreen`（主屏 + 终端自带回滚，聊天式应用首选）/ `TuiAltScreen`（备用屏 + 应用自管滚动 + 布局根 + 鼠标选区/OSC 52 复制/视口内搜索）。

**(3) 假光标与 IME 定位**（终端编辑器最难的问题之一）：隐藏真光标，焦点组件在输出里嵌入零宽标记 `CURSOR_MARKER`，TUI 事后扫描把硬件光标定位过去——否则中文输入法的候选框会出现在错误位置。

**(4) 键盘系统**（`src/keys.ts`，1401 行）：同时支持传统转义序列和 Kitty keyboard protocol（可区分 Shift+Enter 等）；`matchesKey(data, "ctrl+c")` + 编译期 KeyId 类型（拼错编译不过）；`stdin-buffer.ts` 重组分片到达的转义序列。

**(5) 组件清单**：`Container`（拼接）、`Text`/`TruncatedText`、`Input`、**`Editor`（2363 行，最重组件：自动换行、斜杠命令/文件路径补全、大粘贴折叠、undo 栈、Emacs kill-ring）**、`Markdown`、`Loader`/`CancellableLoader`、`SelectList`、`SettingsList`、`Image`（Kitty/iTerm2 图形协议）、`VStack`/`HStack`（flexbox 式布局）、`ScrollView`。另有 Overlay 系统（9 种锚点、ANSI 安全的行合成）。

**(6) 与 React 的异同**：保留了"组件 + 声明式 render + diff"，但 diff 降到**最终字符串行**这一层（比虚拟 DOM 更薄）；无状态框架、无生命周期——组件自管私有字段 + 手动 `invalidate()`；事件靠焦点组件的 `handleInput(原始数据)`；通信靠属性回调。

### 9.3 精读顺序与练习

1. `README.md`（写得极详尽）→ `src/tui.ts`（契约与 TuiBase）→ `src/tui-main-screen.ts` 的 `doRender()`（差分核心）。
2. `src/keys.ts` + `src/stdin-buffer.ts`。
3. `src/components/markdown.ts`、`editor.ts`（选读）。
4. 练习：跑 `packages/tui/test/chat-simple.ts`（最小可运行聊天 UI）；自己用 Container+Text+Loader 组一个流式输出界面。

---

## 十、阶段 5：packages/session-backends/sqlite-node —— SQLite 会话后端（2 天，选学）

### 10.1 解决什么问题

JSONL 文件后端够用但查询慢、无全文检索、多进程并发不安全。SQLite 后端（基于 Node 22+ 内置的 `node:sqlite`，所以独立成包、不进核心）提供生产级存储：

- **实现三个契约**：`SessionRepo`（create/open/list/delete/fork）、`SessionStorage`（agent 包的接口）、`SessionSearch`（FTS5 全文检索，trigram 分词、外容模式、触发器同步）。
- **写者租约（writer lease）防脑裂**：TTL 30 秒 + 心跳续约 + **fence 计数器**——过期持有者恢复后即使写入也会被 fence 拒绝，防止旧进程覆盖新进程的数据。每个写事务先续约再写入。
- **分支缓存物化视图**：entries 的 parentId 链是唯一事实，`branch_entries`/`branch_tips` 缓存表让"沿分支扫描"变廉价，append 时增量维护、可 `repairBranchCache` 重建。
- **操作串行化**：`SerialOperationQueue` 把所有仓库操作排队；WAL 模式 + busy_timeout。
- schema 迁移体系（`migrations/001_initial.sql` + migrations 表）。

### 10.2 精读与练习

1. `README.md` → `src/sqlite/repo.ts`（953 行）→ `migrations/001_initial.sql`（看表结构注释）→ `src/sqlite/branch-cache.ts` → `src/sqlite/search-backend.ts`。
2. 对比 `packages/agent/src/harness/session/memory.ts` / `jsonl/` / 本包：同一个接口的三种实现，体会"可插拔后端"模式的成本与收益。
3. 看 `packages/agent/src/harness/session/testing/conformance.ts`：**一致性测试套件**如何保证三个后端行为等价（这个测试思路可以搬到任何"多后端"项目）。

---

## 十一、阶段 6：protocol / client / server —— 远程会话三件套（3 天，选学）

### 11.1 解决什么问题

让 agent 会话跑在远程机器（或独立进程）上、被浏览器/其他终端消费。三包分工：**protocol 定义字节格式，client 是消费端库，server 是宿主框架**（实验性）。全部传输中立——只要"保序字节流"（WebSocket、Unix socket 都行）。

### 11.2 protocol（先学这个）

- **线格式**：`[uint32 大端长度][一个 CBOR item]`；首帧必须是 `hello`（版本协商）。
- **四层解耦**：typebox schema（拒绝未知字段）→ 自实现的**严格 RFC 8949 CBOR 子集**（自带 16MiB/百万元素/64 层深度限额，防不可信输入）→ 长度前缀分帧（增量解码容忍任意分片）→ 传输抽象。
- **消息全景**（`src/schemas.ts` 一个文件看完）：客户端 9 种命令（list/create/attach/detach/prompt/steer/abort/set_model/set_thinking）；服务端响应 + 4 种事件。
- **最重要的设计决策——快照与进度分离**：`session_snapshot` 是权威状态（revision 单调递增，可重放）；`session_progress`（`assistant_delta` 等流式增量）只是**瞬态 UI 提示，不得折叠进权威状态**。UI 用 progress 做即时反馈、用 snapshot 纠偏，客户端从不乐观更新。这一原则对任何"流式 + 状态"系统都适用。

### 11.3 client

- `PiClient` + 注入的 `ByteTransportFactory`（只需实现 send/close + onData 回调）；连接、握手、请求按 ID 关联；不自动重连（显式 `reconnect()`）。
- **租约（lease）语义**：`exclusive`（变更协调者）与 `shared`（多观察者）两种会话租约；`await using` 式 dispose；最后一个租约释放才发 detach。这是"多消费者安全共享一个远程会话"的生命周期范式。

### 11.4 server（实验性）

- 宿主只需实现 `PiServerService`（listSessions/listModels/createSession/openSession）和 `PiSessionRuntime`（snapshot/prompt/steer/abort/...）；框架负责握手、命令路由、快照广播、错误消毒（内部错误保留 cause、永不序列化细节）。
- 亮点：`src/protocol.ts` 的**编译期字段清单**（`ExactKeys` Assert）——pi-ai 每加一个字段，桥接层编译失败，强制人工审查协议映射。

### 11.5 精读与练习

1. `packages/protocol/src/schemas.ts` → `framing.ts` → `cbor/decoder.ts`。
2. `packages/client/src/client.ts` + `session-handle.ts`。
3. `packages/coding-agent/src/client/remote-session.ts` + `transcript.ts`：看协议在真实 agent 客户端里怎么折叠成转录状态（`applyTranscriptSnapshot` / `applyTranscriptProgress`）。

---

## 十二、telemetry 与 evals（进阶）

### 12.1 packages/telemetry（半天）

零运行时依赖的**厂商中立遥测契约**：回调式 `startSpan(options, callback)`（span 生命周期由 callback 控制，无公开 end()）、显式传参（不用 AsyncLocalStorage 全局状态）、`defineTelemetrySchema()` 编译期校验属性名/闭集值。实际的 agent 词汇表（`pi.ai.*`、`pi.harness.*`）定义在 agent 包的 `src/harness/telemetry.ts`。**学习点**：如何定义一个可被任意后端（OTel/Sentry/日志）实现的观测契约 + 配套一致性测试套件（`src/testing/conformance.ts`）。

### 12.2 packages/evals（按需）

私有评测框架，`npm run eval` 运行。当你想验证"我改的提示词/工具没有退化"时再研究。

---

## 十三、五大核心机制专讲（跨包串讲，供复习与面试式自检）

### 13.1 Agent Loop 与事件流

- **问题**：UI 需要渲染过程（打字机效果、工具进度）、持久化需要落盘点、扩展需要介入点——如果没有统一协议，每加一个消费者就要改循环。
- **方案**：循环只产出一个嵌套事件流（agent/turn/message/tool_execution 三层），所有消费者（UI、持久化、遥测、扩展）都变成订阅者；`Agent` 类的事件屏障保证订阅者全部完成后 run 才结算。
- **自检**：`stopReason` 有哪几种？`length` 时为什么整批工具调用要标记失败？steering 消息在哪两个时机被注入？

### 13.2 工具系统

- **问题**：工具要同时面对模型（schema）、UI（渲染）、安全（拦截）、并发（同文件写冲突）四个世界。
- **方案**：一个 `AgentTool` 接口承载全部（parameters schema / execute / onUpdate 进度 / details 结构化数据不进模型上下文）；三段式执行（预检-并发-收尾）天然给出拦截点（beforeToolCall）与改写点（afterToolCall）；`withFileMutationQueue` 按 canonical path 串行化同文件写。
- **自检**：工具失败为什么必须 throw 而不是返回错误文本？details 和 content 的区别是什么？

### 13.3 上下文压缩（compaction）

- **问题**：长会话必然超出窗口；丢历史会让 agent"失忆"；压错位置（切在 toolCall 和 toolResult 之间）会让模型困惑。
- **方案**：token 估算（usage 锚点 + 尾部估算）→ 合法切点（消息边界 + 回合边界对齐 + split-turn 前缀单独摘要）→ 结构化六段式摘要（增量更新）+ 文件操作清单 → compaction Entry 存树（原历史不删）。触发三路径：手动 / 阈值 / 溢出恢复。
- **自检**：为什么保留 20000 token 的近期消息？摘要为什么要固定格式？

### 13.4 会话树与持久化

- **问题**：用户想"回到之前某次回答重新来"、程序崩溃后要恢复、多进程可能同时写。
- **方案**：只追加的 Entry 树（parentId 分叉）+ LaneRecord 操作日志（崩溃重放）分离；`SessionStorage`/`SessionRepo` 接口下可插拔（内存/JSONL/SQLite）；SQLite 后端用写者租约（TTL+fence）防脑裂。
- **自检**：为什么"树"和"操作日志"要分成两种存储？分支缓存为什么是可重建的派生数据？

### 13.5 扩展系统与安全模型

- **问题**：功能膨胀会毁掉核心（MCP、权限、plan mode 每个都有人要）；仓库里的 `.pi/extensions/` 是任意代码执行入口。
- **方案**：45 种生命周期事件 + 注册 API 构成扩展面，一切"非核心"功能降维为扩展（子 agent、plan mode、权限门、沙箱都有官方示例）；安全上：项目信任把住"项目输入加载"、`tool_call` 事件把住"工具执行"、容器把住"OS 边界"。
- **自检**：为什么 pi 拒绝内置 MCP？权限弹窗为什么可以用 `tool_call` 事件实现？

---

## 十四、学习里程碑与自测清单

- **里程碑 1（阶段 1 后）**：能手写 50 行脚本用 pi-ai 完成一次流式对话 + 一次工具调用；能说出 AssistantMessageEvent 的种类和"错误不抛出"约定。
- **里程碑 2（阶段 2 后）**：能不看资料画出 agent loop 的完整流程图（含 steering/abort/followUp 注入点、工具三段式、compaction 触发）；能解释事件屏障为什么存在。
- **里程碑 3（阶段 3 后）**：能给 pi 写一个含自定义工具 + 斜杠命令 + tool_call 拦截的扩展；能讲清系统提示词的八段组装和 AGENTS.md 注入路径；能读懂一个会话 JSONL 文件。
- **里程碑 4（阶段 4~6 后，选）**：能解释差分渲染管线；能说出快照/进度分离原则；能描述写者租约的防脑裂机制。
- **终局检验**：合上文档，从 `用户按 Enter` 开始，把第三节的生命周期图完整复述一遍，每一步都能指出对应源码文件。

---

## 十五、动手实践项目建议（把学到的东西串起来）

1. **迷你 agent CLI**（阶段 2 后）：pi-ai + agent 包，100~200 行，支持一个 bash 工具和流式打印——你会遇到并解决"输出截断""错误回喂"等真实问题。
2. **上下文压缩实验**（阶段 2/3 后）：调大调小 reserveTokens/keepRecentTokens，构造超长会话，观察摘要质量与切点行为。
3. **实用扩展**（阶段 3 后）：给 pi 写一个你真的会用的扩展（如"提交前自动跑 lint 的权限门"、"自定义 Jira 工具"），体会扩展 API 的边界。
4. **换一个持久化后端**（阶段 5 后）：参照 conformance 测试实现一个自己的 `SessionStorage`（哪怕是写日志的假后端），检验对会话模型的理解。
5. **读一份大设计文档**（进阶）：`packages/agent/docs/harness.md`（2941 行的下一代架构规格：操作状态机、崩溃恢复、writer lease 的理论依据）——读完后你会理解为什么当前代码里有些接口"看起来空着"。

---

## 附录 A：全局快速索引（按"我想学 X"查找）

| 我想学… | 去看 |
| --- | --- |
| agent 循环本体 | `packages/agent/src/agent-loop.ts` |
| 事件流协议 | `packages/agent/src/types.ts`（AgentEvent）+ README 时序图 |
| 工具怎么定义/执行 | `packages/agent/src/types.ts`（AgentTool）+ `packages/coding-agent/src/core/tools/` |
| 中断/插话 | `packages/agent/src/agent.ts`（steer/abort/followUp） |
| 上下文压缩 | `packages/agent/src/harness/compaction/compaction.ts` + `docs/compaction.md`（coding-agent） |
| 会话格式/分支 | `packages/coding-agent/src/core/session-manager.ts` + `docs/session-format.md` |
| 系统提示词组装 | `packages/coding-agent/src/core/system-prompt.ts` |
| AGENTS.md 注入 | `packages/coding-agent/src/core/resource-loader.ts` |
| Skills 机制 | `packages/agent/src/harness/skills.ts` + `docs/skills.md` |
| 扩展 API 全参考 | `packages/coding-agent/src/core/extensions/types.ts` + `docs/extensions.md` |
| 权限/安全 | `docs/security.md` + `examples/extensions/permission-gate.ts` |
| 子 agent / plan mode | `examples/extensions/subagent/`、`plan-mode/` |
| LLM 协议适配 | `packages/ai/src/api/anthropic-messages.ts`（或 openai-completions） |
| 跨模型会话迁移 | `packages/ai/src/api/transform-messages.ts` |
| 流式 UI 渲染 | `packages/tui/src/tui-main-screen.ts` + `components/markdown.ts` |
| 远程会话协议 | `packages/protocol/src/schemas.ts` |
| 多进程写安全 | `packages/session-backends/sqlite-node/src/sqlite/repo.ts`（writer lease） |
| 崩溃恢复设计 | `packages/agent/src/harness/reducer.ts` + `docs/harness.md` |
| 渐进式 SDK 教程 | `packages/coding-agent/examples/sdk/01~13` |

## 附录 B：注意事项

- 本仓库规范：ESM-only、Node >= 22.19、只用可擦除 TypeScript 语法（无 enum/namespace/参数属性）；`models.generated.ts` 禁止手改。
- 改代码后跑 `npm run check`；跑测试用 `./test.sh`（隔离环境）而非 `npm test`。
- 多会话可能同时在同一目录工作，git 操作只碰自己改的文件，禁止 `git add -A` / `git reset --hard` 等。
- `packages/server` 是实验性包，接口可能随时变；学习时以 protocol/client 为主。
